{
  "name": "reteworld",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm install && cd 'rete-admin' && npm install && npm run build && cd .. && cd rete-web && npm install && cd ..",
    "dev": "concurrently \"nodemon server.js\" \"cd rete-admin && npm run develop\" \"cd rete-web && npm run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.4"
  }
}
