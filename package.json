{
  "name": "reteworld",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clone": "git clone https://github.com/Yondo202/rete-web.git && git clone https://github.com/Yondo202/rete-admin.git",
    "install-all": "npm install && cd 'rete-admin' && npm install && npm run build && cd .. && cd rete-web && npm install && cd ..",
    "dev": "concurrently \"nodemon server.js\" \"cd rete-admin && npm run develop\" \"cd rete-web && npm run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^5.2.0",
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.24",
    "nodemon": "^2.0.4",
    "validator": "^13.1.1"
  }
}
